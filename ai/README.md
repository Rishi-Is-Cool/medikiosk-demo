# AI Layer

MediKiosk AI/ML components.

## Structure

- `intake/` — voice transcription, clinical extraction, question-flow logic, and schemas
- `common/` — shared AI utilities and configuration
- `tests/` — AI pipeline tests and sample inputs

## Current priority

Build the conversational intake pipeline:

`audio/text → transcript → structured clinical JSON`
